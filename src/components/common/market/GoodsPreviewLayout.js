const { default: styled } = require('styled-components');
const { rem } = require('../../../lib/styles/variables');

const GoodsPreviewLayout = styled.section`
  margin-top: ${rem(50)};
`;

export default GoodsPreviewLayout;
